import 'package:flutter/material.dart';

import './number_verify.dart';

class Authentication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return NumberVerifyScreen();
  }
}
